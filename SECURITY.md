# 安全说明

## 报告漏洞

**不要开公开 issue。** 请用 GitHub 的私密漏洞报告（仓库页面 → Security → Report a vulnerability），
或者开一个只说「我想报一个安全问题，请给我私密渠道」的 issue。

请附上：影响版本、复现步骤、最小 PoC、影响面（能否逃出允许根目录 / 绕过拒绝清单 / 绕过权限闸门）。

## 这个插件会做什么

- 在 DSH 宿主进程里**直接持有 MCP 连接**，把远端服务声明的工具注册进 ctx.tools；
- 按需拉起 MCP 子进程（内置服务用 process.execPath，外部预设常用 npx / uvx）；
- 提供一个持久终端会话（默认 PowerShell 或 bash），以及内置的 files / exec MCP 服务。

## 已写死的边界

| 面 | 边界 |
|---|---|
| 文件 | 默认只允许家目录 + 临时目录；realpath 之后仍要落在允许根内；删改需要 destructive: true；系统目录只读 |
| 命令 | 平台化危险命令拒绝清单（POSIX 一套、Windows 一套）；超时与输出上限 |
| 终端 | 与 exec 共用拒绝清单；单条输出 512 KiB 上限；插件卸载时全部收掉 |
| 权限 | 四级（永久 / 本会话 / 每次询问 / 禁用）；无审批通道时**失败关闭**，绝不放行 |
| 子进程 | 继承插件进程权限，**不做任何提权** |

## 不在范围内

- 你**主动安装**的第三方 MCP 服务自己会做什么（它们就是普通子进程，请自行评估来源）；
- 用户显式配置 MCP_EXEC_DENY / MCP_FS_ROOTS 放宽或收紧策略后的行为；
- DSHA 手机侧能力的授权开关（由 App 与 Android 权限体系管）。
